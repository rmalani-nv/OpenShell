# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

suite: gateway TOML config shape
templates:
  - templates/gateway-config.yaml
  - templates/statefulset.yaml
  - templates/db-secret.yaml
release:
  name: openshell
  namespace: my-namespace

tests:
  # Regression for Drew's P2: a ConfigMap-only mutation in `helm upgrade`
  # must roll the StatefulSet, otherwise pods keep running with stale config.
  - it: annotates the StatefulSet pod template with a ConfigMap checksum
    template: templates/statefulset.yaml
    asserts:
      - exists:
          path: spec.template.metadata.annotations["checksum/gateway-config"]

  - it: mounts the OIDC CA bundle when TLS is disabled
    template: templates/statefulset.yaml
    set:
      server.disableTls: true
      server.oidc.issuer: https://issuer.example.com
      server.oidc.caConfigMapName: openshell-oidc-ca
    asserts:
      - equal:
          path: spec.template.spec.containers[0].volumeMounts[3].name
          value: oidc-ca
      - equal:
          path: spec.template.spec.containers[0].volumeMounts[3].mountPath
          value: /etc/openshell-tls/oidc-ca
      - equal:
          path: spec.template.spec.volumes[2].name
          value: oidc-ca
      - equal:
          path: spec.template.spec.volumes[2].configMap.name
          value: openshell-oidc-ca

  # Regression for the P1 bug Drew flagged: grpc_endpoint MUST live in the
  # Kubernetes driver table, not in [openshell.gateway]. The gateway-side
  # schema has `deny_unknown_fields` and no `grpc_endpoint` field, so writing
  # it at gateway scope makes `config_file::load` reject the default install.
  - it: renders grpc_endpoint under [openshell.drivers.kubernetes], not [openshell.gateway]
    template: templates/gateway-config.yaml
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: '(?ms)\[openshell\.drivers\.kubernetes\].*?grpc_endpoint'
      - notMatchRegex:
          path: data["gateway.toml"]
          pattern: '(?ms)\[openshell\.gateway\][^\[]*?grpc_endpoint'

  - it: renders the sandbox service account name under [openshell.drivers.kubernetes]
    template: templates/gateway-config.yaml
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: '(?ms)\[openshell\.drivers\.kubernetes\].*?service_account_name\s*=\s*"openshell-sandbox"'

  - it: does not render local mTLS user auth for Kubernetes deployments
    template: templates/gateway-config.yaml
    asserts:
      - notMatchRegex:
          path: data["gateway.toml"]
          pattern: '\[openshell\.gateway\.mtls_auth\]'

  - it: does not allow unauthenticated users by default
    template: templates/gateway-config.yaml
    asserts:
      - notMatchRegex:
          path: data["gateway.toml"]
          pattern: '\[openshell\.gateway\.auth\]'

  - it: renders explicit unauthenticated user dev mode when enabled
    template: templates/gateway-config.yaml
    set:
      server.auth.allowUnauthenticatedUsers: true
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: '(?ms)\[openshell\.gateway\.auth\].*?allow_unauthenticated_users\s*=\s*true'

  - it: uses the configured existing sandbox service account name
    template: templates/gateway-config.yaml
    set:
      sandboxServiceAccount.create: false
      sandboxServiceAccount.name: precreated-sandbox
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: '(?ms)\[openshell\.drivers\.kubernetes\].*?service_account_name\s*=\s*"precreated-sandbox"'

  - it: omits server_sans when no DNS SANs are configured
    template: templates/gateway-config.yaml
    asserts:
      - notMatchRegex:
          path: data["gateway.toml"]
          pattern: 'server_sans\s*='

  - it: emits disable_tls=true and omits the [openshell.gateway.tls] section when disableTls is set
    set:
      server.disableTls: true
      certManager.enabled: false
      pkiInitJob.enabled: false
    template: templates/gateway-config.yaml
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: 'disable_tls\s*=\s*true'
      - notMatchRegex:
          path: data["gateway.toml"]
          pattern: '\[openshell\.gateway\.tls\]'

  - it: renders server_sans from certManager.serverDnsNames
    set:
      certManager.enabled: true
      certManager.serverDnsNames:
        - openshell
        - "*.dev.openshell.localhost"
      pkiInitJob.enabled: false
    template: templates/gateway-config.yaml
    asserts:
      - matchRegex:
          path: data["gateway.toml"]
          pattern: 'server_sans\s*=\s*\["openshell", "\*\.dev\.openshell\.localhost"\]'

  - it: passes sqlite db-url via --db-url arg by default
    template: templates/statefulset.yaml
    asserts:
      - contains:
          path: spec.template.spec.containers[0].args
          content: "sqlite:/var/openshell/openshell.db"

  - it: does not create a db Secret when postgres is disabled
    template: templates/db-secret.yaml
    asserts:
      - hasDocuments:
          count: 0

  - it: does not pass --db-url in args when postgres is enabled
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: true
      postgres.auth.password: test-pw
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].args
          content: "--db-url"

  - it: references Bitnami service-binding Secret when postgres.deploy is true
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: true
      postgres.auth.password: test-pw
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: OPENSHELL_DB_URL
            valueFrom:
              secretKeyRef:
                name: openshell-postgres-svcbind-custom-user
                key: uri

  - it: respects postgres.fullnameOverride for bundled service-binding Secret
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: true
      postgres.auth.password: test-pw
      postgres.fullnameOverride: my-pg
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: OPENSHELL_DB_URL
            valueFrom:
              secretKeyRef:
                name: my-pg-svcbind-custom-user
                key: uri

  - it: respects postgres.nameOverride for bundled service-binding Secret
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: true
      postgres.auth.password: test-pw
      postgres.nameOverride: pgdb
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: OPENSHELL_DB_URL
            valueFrom:
              secretKeyRef:
                name: openshell-pgdb-svcbind-custom-user
                key: uri

  - it: references chart-created Secret for external postgres
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: false
      postgres.external.host: pg.example.com
      postgres.external.password: ext-pw
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: OPENSHELL_DB_URL
            valueFrom:
              secretKeyRef:
                name: openshell-db
                key: uri

  - it: references existing Secret when existingSecret is set
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.external.existingSecret: my-pg-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: OPENSHELL_DB_URL
            valueFrom:
              secretKeyRef:
                name: my-pg-secret
                key: uri

  - it: does not create db Secret when postgres.deploy is true
    template: templates/db-secret.yaml
    set:
      postgres.enabled: true
      postgres.deploy: true
      postgres.auth.password: test-pw
    asserts:
      - hasDocuments:
          count: 0

  - it: does not create db Secret when existingSecret is set
    template: templates/db-secret.yaml
    set:
      postgres.enabled: true
      postgres.external.existingSecret: my-pg-secret
    asserts:
      - hasDocuments:
          count: 0

  - it: creates db Secret with individual keys for external postgres
    template: templates/db-secret.yaml
    set:
      postgres.enabled: true
      postgres.deploy: false
      postgres.external.host: external-postgres.example.com
      postgres.external.port: 5432
      postgres.external.database: openshell_ext
      postgres.external.username: ext_user
      postgres.external.password: ext_pass
    asserts:
      - isKind:
          of: Secret
      - equal:
          path: data.host
          decodeBase64: true
          value: "external-postgres.example.com"
      - equal:
          path: data.username
          decodeBase64: true
          value: "ext_user"
      - equal:
          path: data.password
          decodeBase64: true
          value: "ext_pass"
      - equal:
          path: data.database
          decodeBase64: true
          value: "openshell_ext"
      - equal:
          path: data.uri
          decodeBase64: true
          value: "postgresql://ext_user:ext_pass@external-postgres.example.com:5432/openshell_ext"

  - it: URL-encodes special characters in external credentials
    template: templates/db-secret.yaml
    set:
      postgres.enabled: true
      postgres.deploy: false
      postgres.external.host: pg.example.com
      postgres.external.username: "user@corp"
      postgres.external.password: "p@ss:word/secret"
      postgres.external.database: mydb
    asserts:
      - equal:
          path: data.uri
          decodeBase64: true
          value: "postgresql://user%40corp:p%40ss%3Aword%2Fsecret@pg.example.com:5432/mydb"

  - it: fails when postgres.deploy=true but postgres.enabled=false
    template: templates/statefulset.yaml
    set:
      postgres.deploy: true
    asserts:
      - failedTemplate:
          errorMessage: "postgres.deploy=true requires postgres.enabled=true"

  - it: fails when external postgres has no password and no existingSecret
    template: templates/statefulset.yaml
    set:
      postgres.enabled: true
      postgres.deploy: false
      postgres.external.host: my-host.example.com
    asserts:
      - failedTemplate:
          errorMessage: "postgres.external.password is required when postgres.deploy=false and no postgres.external.existingSecret is provided"
